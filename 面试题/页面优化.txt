webpack 优化项目
1、对于 Webpack4，打包项目使用 production 模式，这样会自动开启代码压缩
2、使用 ES6 模块来开启 tree shaking，这个技术可以移除没有使用的代码
3、优化图片，对于小图可以使用 base64 的方式写入文件中
4、按照路由拆分代码，实现按需加载
5、给打包出来的文件名添加哈希，实现浏览器缓存文件

图片加载优化
1、小图使用 base64 格式
2、将多个图标文件整合到一张图片中（雪碧图）
3、iconfont

懒加载
懒加载就是将不关键的资源延后加载，占位符

1、减少http请求
2、合理设置浏览器缓存 （服务器中配置）
cache-control  对很少变化的文件进行浏览器缓存
expires 设置过期时间